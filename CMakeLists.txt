PROJECT(khaki)

AUX_SOURCE_DIRECTORY(. DIR_SRCS)
INCLUDE_DIRECTORIES(deps/include)
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -std=c++11")
ADD_LIBRARY(khaki STATIC ${DIR_SRCS})
TARGET_LINK_LIBRARIES(khaki deps/lib/liblog4cpp.a)
